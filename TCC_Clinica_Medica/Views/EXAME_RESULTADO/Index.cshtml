@model IEnumerable<TCC_Clinica_Medica.EXAME_RESULTADO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DESCRICAO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DATA_CRIACAO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ENTREGUE_PACIENTE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EXAMES_SOLICITADOS.ID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DESCRICAO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DATA_CRIACAO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ENTREGUE_PACIENTE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EXAMES_SOLICITADOS.ID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
