@model IEnumerable<TCC_Clinica_Medica.RECEITAS>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CONSULTAS.ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MEDICAMENTOS.NOME_GENERICO)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CONSULTAS.ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MEDICAMENTOS.NOME_GENERICO)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
